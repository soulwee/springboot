#默认环境
server:
  port: 8900
spring:
  messages:
    basename: i18n/login
  #redis
  redis:
    host: 127.0.0.1
    port: 6379
   #实现热部署
  devtools:
    restart:
      enabled: true
      #设置重启的目录,paths添加哪个目录的文件需要restart，exclude不需要
      additional-paths: src/main/java
      additional-exclude: WEB-INF/**
  #环境设置：dev、test、prod
  profiles:
    active: dev
  mvc:
    view:
      prefix: /
      suffix: .jsp
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    username: lms
    password: oracle
    url: jdbc:oracle:thin:@192.168.2.26:1521:jiexinv68
#    username: root
#    password: 123456
#    url: jdbc:mysql://localhost:3306/spring_cache?serverTimezone=GMT%2B8
#    driver-class-name: com.mysql.jdbc.Driver
#    type: com.alibaba.druid.pool.DruidDataSource

# 配置完yml并没有生效,需要 使用DataSource池进行自动配置（加入配置类DruidConfig）
    filters: stat
    maxActive: 20
    initialSize: 1
    maxWait: 60000
    minIdle: 1
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: select 1 from dual
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxOpenPreparedStatements: 20

###mybatis配置
mybatis:
  type-aliases-package: com.seecen.entity
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
     #开启MyBatis的二级缓存，一级缓存默认开启
    cache-enabled: true
  mapper-locations: classpath:mappers/*.xml
pagehelper:
  reasonable: true
  support-methods-arguments: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
#日志打印
logging:
  level:
    com.seecen: debug
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

